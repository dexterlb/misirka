[project]
name = "misirka"
version = "0.1.0"
description = "Realtime RPC"
readme = "../README.md"
requires-python = ">=3.9"
dependencies = [
]

[project.scripts]
ug2cho = "msksrvwrapper.test_toy:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/msksrvwrapper"]
