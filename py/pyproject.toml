[project]
name = "misirka"
version = "0.1.0"
description = "Realtime RPC"
readme = "../README.md"
requires-python = ">=3.9"
dependencies = [
]

[project.scripts]
test_toy = "misirka.srv_wrapper.test_toy:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/misirka"]
