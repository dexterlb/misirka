### misirka

`misirka` is an opinionated library for writing realtime control APIs.

**server implementations**:
- go
- python (currently a thin shim that uses the go server as a subprocess)

**client implementations**:
- typescript

At some point we must properly document this library, but we have not yet done this.
